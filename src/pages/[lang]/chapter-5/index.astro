---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import type { Language } from "../../../lib/i18n";
import { getTranslations } from "../../../lib/i18n";

import EducationSection from "./_components/EducationSection.astro";
import HealthSection from "./_components/HealthSection.astro";
import WaterSanitationSection from "./_components/WaterSanitationSection.astro";

export async function getStaticPaths() {
    return [{ params: { lang: "ne" } }, { params: { lang: "en" } }];
}

const { lang } = Astro.params as { lang: Language };
const t = getTranslations(lang);

const pageTitle =
    lang === "ne" ? "परिच्छेद – ५: सामाजिक अवस्था" : "Chapter 5: Social Status";
---

<BaseLayout
    lang={lang}
    title={pageTitle}
    description={t.chapters[5].description}
    currentChapter={5}
>
    <section
        class="bg-gradient-to-r from-teal-600 to-teal-800 text-white py-16"
    >
        <div class="container">
            <div class="max-w-4xl">
                <div class="text-teal-200 mb-2 font-medium">
                    {lang === "ne" ? "परिच्छेद ५" : "Chapter 5"}
                </div>
                <h1 class="text-4xl md:text-5xl font-bold mb-4">
                    {pageTitle}
                </h1>
                <p class="text-xl text-teal-100">
                    {t.chapters[5].description}
                </p>
            </div>
        </div>
    </section>

    <section class="section-spacing">
        <div class="container">
            <div class="max-w-6xl mx-auto">
                <EducationSection lang={lang} />
                <HealthSection lang={lang} />
                <WaterSanitationSection lang={lang} />
            </div>
        </div>
    </section>

    <section class="border-t border-gray-200 py-8">
        <div class="container">
            <div class="flex justify-between items-center max-w-6xl mx-auto">
                <a
                    href={`/${lang}/chapter-4`}
                    class="btn btn-secondary flex items-center gap-2"
                >
                    <svg
                        class="w-5 h-5"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                        ><path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M15 19l-7-7 7-7"></path></svg
                    >
                    <span>{t.labels.previousChapter}</span>
                </a>
                <a
                    href={`/${lang}/chapter-6`}
                    class="btn btn-primary flex items-center gap-2"
                >
                    <span>{t.labels.nextChapter}</span>
                    <svg
                        class="w-5 h-5"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                        ><path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M9 5l7 7-7 7"></path></svg
                    >
                </a>
            </div>
        </div>
    </section>
</BaseLayout>
