---
import type { Language } from "../../../lib/i18n";
import ne from "../../../lib/i18n/chapters/5/major-diseases/ne.json";
import en from "../../../lib/i18n/chapters/5/major-diseases/en.json";

interface Props {
    lang: Language;
}

const { lang } = Astro.props;
const t = lang === "ne" ? ne : en;
---

<section
    id={t.subsectionId}
    class="relative py-20 bg-[#0a0a0a] border-t border-white/5"
>
    <div class="container mx-auto px-8 relative z-10">
        <!-- Main Title -->
        <h2 class="text-2xl md:text-3xl font-serif text-white/90 mb-8">
            {t.title}
        </h2>

        <!-- Content -->
        <p class="text-lg text-zinc-300 leading-relaxed mb-12 text-justify">
            {t.content}
        </p>

        <!-- Table -->
        <div class="mb-8 w-full">
            <h3 class="text-lg font-serif text-white/80 mb-6 text-center">
                {t.tableTitle}
            </h3>
            <div class="rounded-lg overflow-hidden border border-zinc-800">
                <div class="overflow-x-auto">
                    <table
                        class="w-full text-sm text-center text-zinc-100 border-collapse"
                    >
                        <thead class="bg-orange-500/80">
                            <tr>
                                <th
                                    class="px-6 py-4 font-serif font-medium text-white border-r border-white/20 w-24"
                                >
                                    {t.headers.sn}
                                </th>
                                <th
                                    class="px-6 py-4 font-serif font-medium text-white text-left"
                                >
                                    {t.headers.disease}
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            {
                                t.tableData.map((row, index) => (
                                    <tr
                                        class={`border-b border-zinc-800 ${index % 2 === 0 ? "bg-zinc-900/30" : "bg-transparent"} hover:bg-zinc-800/50 transition-colors`}
                                    >
                                        <td class="px-6 py-4 text-zinc-300 font-medium border-r border-zinc-800">
                                            {row.sn}
                                        </td>
                                        <td class="px-6 py-4 text-left font-medium text-white">
                                            {row.disease}
                                        </td>
                                    </tr>
                                ))
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <p class="text-sm text-zinc-400 mt-2 text-right italic">
                {t.source}
            </p>
        </div>
    </div>
</section>
